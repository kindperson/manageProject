<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="example.dao.StudentMapper">

    <select id="getAll" resultType="example.pojo.Student">
        select id,name,grade,score,age from student;
    </select>
</mapper>